# Operator notes — Harris County, Texas

Generated by `scaffold/bootstrap_county.py` on 2026-07-11 09:05:52 UTC.
Framework version: v5.1.2-beta-r3

This file captures operator-volunteered knowledge during Phase 0 that does
NOT rise to the level of a schema-recorded operator override. It is a
contextual companion to `config/counties/harris_tx.json`, not a
schema-validated artifact.

See MASTER_PROMPT.md Section 4.30 for the rule. Two channels with a clean
separation of concerns:

- **`operator_override_audit`** (in the county config JSON) — records
  every operator override that CHANGES what the framework is allowed
  to do.
- **`operator_notes.md`** (this file) — records CASUAL operator
  knowledge that doesn't change framework behavior but is worth
  keeping for context.

Claude Code MUST append to this file during Phase 0 as operator
knowledge surfaces. Each entry should be:

- Timestamped with ISO 8601 UTC
- Under a section heading for the relevant source ID (or `## general`)
- Briefly summarized in Claude Code's own words

## general

- 2026-07-11T00:00:00Z — Agent name is "Luna" (operator renamed from the
  framework default "Harris"). Address self as Luna in this profile.
- 2026-07-11T00:00:00Z — `WRITE CONFIG` was an explicit operator approval to run
  `scaffold/ops/write_county_config.py` (Phase 0 Step 4). This is the only code
  run authorized so far; no scrapers/dashboards/Build Mode approved.
- 2026-07-11T00:00:00Z — TinyFish Browser API key supplied by operator for use on
  403/504-blocked sources (route via POST api.browser.tinyfish.ai -> cdp_url ->
  Playwright connect_over_cdp). Key stored at /root/.tinyfish_key (chmod 600,
  OUTSIDE the repo). Do NOT echo. Browser automation backend in the sandbox
  returned 502 during recon; TinyFish is the fallback for blocked portals.
- 2026-07-11T00:00:00Z — Web search (Google) was CAPTCHA-walled for the sandbox
  IP. Discovery leaned on direct navigation + official in-page link extraction
  instead. All primary sources still discovered this way.

## harris_clerk_real_property

- 2026-07-11T00:00:00Z — Real Clerk domain is `cclerk.hctx.net`. `harriscountyclerk.com`
  is a LOOKALIKE/phishing risk (it redirected to a "Security Check / I'm a human"
  page on a non-county domain `view.secure-password.online`). Excluded. Do not use.

## harris_dc_court_records

- 2026-07-11T00:00:00Z — Confirmed session-gated: direct load of the search URL
  returns "log back in and try again". Loading the homepage first (sets a cookie
  via 302) then reaching the search page CLEARS the gate — verified by curl
  (homepage 302 -> search page returns the real form). Scraper should navigate
  home-first, capture the cookie, then search. No login/payment needed.

## harris_sheriff_sales

- 2026-07-11T00:00:00Z — Official URL NOT pinned in recon. County homepage and
  Sheriff dept page linked only to Constable Precincts (Harris has 8 precincts
  that each run sales). Bare /TaxSales/ on the tax site returns 403; the correct
  tax-sale path is /Property/TaxSales/Index. Next session: pin the Sheriff/
  Constable foreclosure-sale URL via harriscountytx.gov navigation or TinyFish if
  bot-blocked. Operator chose to leave this UNVERIFIED + enabled=false for now.
